/**
 * @file Presentation domain module exports
 *
 * Presentation resource types and utilities.
 * No render layer dependencies.
 */

// Types
export type {
  SlideSizeType,
  SlideSize,
  CustomShow,
  SlideShowRange,
  BrowseShowProperties,
  KioskShowProperties,
  PresentShowProperties,
  ShowProperties,
  PresentationProperties,
  SlideSyncProperties,
  ModifyVerifier,
  PhotoAlbumFrameShape,
  PhotoAlbumLayout,
  PhotoAlbum,
  SmartTags,
  Presentation,
} from "./types";

export type {
  SlideSizeEmu,
  NotesSizeEmu,
  SlideIdEntry,
  SlideMasterIdEntry,
  NotesMasterIdEntry,
  HandoutMasterIdEntry,
} from "./structure";
